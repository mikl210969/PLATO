# 1. Проверяем статус изменений (убедимся, что все новые файлы и правки подхвачены)
git status

# 2. Добавляем все изменения в индекс
git add .

# 3. Делаем коммит с подробным и профессиональным описанием
git commit -m "feat: Завершение Спринта 5 - Breakout и Absorption стратегии с гибридным исполнением

- BreakoutDetector: новый модуль, отслеживающий поедание стен (порог 60%, скорость 5%/с, защита от подлива 50%).
- BreakoutStrategyV1: событийная стратегия пробоя с анализом TrendContext и оценкой пустоты ликвидности за стеной.
- Гибридное исполнение: расширение EnrichedSignal (order_type, execution_params) для поддержки логики limit -> timeout -> market fallback.
- AbsorptionStrategyV2: успешно протестирована и подтверждена генерация реальных сигналов.
- Архитектура: все новые модули (Trend, Absorption, Breakout) корректно интегрированы в AnalyticsHub.
- Чистота кода: удалены отладочные print, логи приведены к профессиональному виду."

# 4. Отправляем изменения в удаленный репозиторий
git push origin main



# 1. Переходим в папку проекта (если ты еще не в ней)
cd C:\Users\user\PLATO31\PLATO

# 2. Проверяем текущий статус (нет ли незакоммиченных изменений)
git status

# 3. Забираем последние изменения с GitHub
git pull origin main



# Сначала гарантированно убиваем старые процессы (на случай, если они висят)
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2

# Запускаем платформу с записью в файл (режим -Append добавляет новые строки, не стирая старые)
python main.py | Tee-Object -FilePath platform_run.log -Append



# Убиваем старые процессы
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2

# Запускаем и перенаправляем ВСЕ потоки (stdout, stderr, warnings) в файл
python main.py *> platform_run.log