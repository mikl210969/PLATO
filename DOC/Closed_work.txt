# 1. Переходим в папку платформы
cd C:\Users\m.ongudushev\proga\PLATO

# 2. Смотрим, что изменилось
git status

# 3. Добавляем ВСЕ изменения (кроме того, что в .gitignore)
git add .

# 4. Делаем коммит с понятным описанием
git commit -m "- [trading/event_handlers.py] Исправлен импорт `datetime` (было `datetime.datetime.now(...)`, стало `datetime.now(timezone.utc)`)
- [trading/event_handlers.py] Реализована логика `SYNC_REQUEST` — синхронизация паспорта с биржей через REST
"

# 5. Отправляем изменения в Git (на GitHub)
git push origin main