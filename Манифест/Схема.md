graph TB
    subgraph UserLayer["👤 ПОЛЬЗОВАТЕЛЬСКИЙ СЛОЙ"]
        Config["📁 Конфиги<br/>config/*.json"]
        Logs["📝 Логи<br/>logs/*.jsonl"]
        Monitor["📊 Мониторинг<br/>Telegram/Email"]
    end

    subgraph StableCore["🔵 STABLE CORE (неприкосновенное ядро)"]
        subgraph Orchestrator[" Главный оркестратор"]
            EventBus[" EventBus<br/>шина событий"]
            Lifecycle["🔄 Lifecycle<br/>запуск/остановка"]
        end

        subgraph Adapters["🔌 Адаптеры"]
            REST["🌐 REST клиент<br/>ордера + данные"]
            WS["📡 WS адаптер<br/>рынок + аккаунт"]
            Normalizer[" Нормализатор<br/>MarketEvent"]
        end

        subgraph TradingCore[" Торговое ядро"]
            Passport[" Passport<br/>состояние сделки"]
            StateMgr["🔀 State Manager<br/>контроль статусов"]
            RiskBasic["🛡️ Базовый Risk<br/>простой SL/TP"]
            LifecycleMgr["⏱️ Lifecycle Mgr<br/>TTL ордеров"]
        end
    end

    subgraph Extensions["🟠 EXTENSIONS (расширения)"]
        subgraph Analytics["📊 Analytics Engine"]
            Candles["🕯️ Свечи<br/>ATR, волатильность"]
            OrderBook["📚 Стакан<br/>HVN, уровни"]
            Imbalance["⚖️ Imbalance<br/>дисбаланс"]
            Whales["🐋 Whale Detector<br/>крупные сделки"]
        end

        subgraph Strategies["🎯 Стратегии"]
            WallFade["🧱 WallFade<br/>отскок от стены"]
            Breakout["💥 Breakout<br/>пробой"]
            Absorption["🌀 Absorption<br/>поглощение"]
        end

        subgraph AdvancedRisk["🎲 Advanced Risk"]
            Grades["🏆 Грейды<br/>A/B/C/REJECT"]
            StaircaseSL["🪜 Ступенчатый SL<br/>SL1 + SL2"]
            BasisStop["📉 Basis Stop<br/>контроль basis"]
            Confidence["✅ Confidence Score<br/>оценка сигнала"]
        end
    end

    subgraph DataStorage[" СЛОЙ ХРАНЕНИЯ ДАННЫХ"]
        HotStorage[("🔥 Hot Storage<br/>SQLite<br/>текущие метрики")]
        ColdStorage[("❄️ Cold Storage<br/>Parquet/JSONL<br/>история")]
    end

    %% Связи пользовательского слоя
    Config --> Orchestrator
    Logs <--> Orchestrator
    Monitor <--> Orchestrator

    %% Связи внутри Stable Core
    Orchestrator --> Adapters
    Adapters --> TradingCore
    TradingCore --> EventBus
    EventBus --> Orchestrator

    %% Связи Stable Core с БД
    Adapters -->|сырые данные| ColdStorage
    TradingCore -->|факты сделок| ColdStorage

    %% Связи Extensions
    Analytics --> Strategies
    Strategies --> EventBus
    EventBus --> TradingCore
    TradingCore --> AdvancedRisk
    AdvancedRisk -->|финальные параметры| TradingCore

    %% Связи Extensions с БД
    ColdStorage -->|история для расчетов| Analytics
    Analytics -->|метрики| HotStorage
    HotStorage -->|чтение метрик| Strategies
    HotStorage -->|чтение метрик| AdvancedRisk

    %% Внешние системы
    Binance[("🏦 Binance/Bybit")] <-->|WS + REST| Adapters

    %% Стилизация
    style StableCore fill:#e1f5ff,stroke:#01579b,stroke-width:3px
    style Extensions fill:#fff3e0,stroke:#e65100,stroke-width:3px
    style DataStorage fill:#f3e5f5,stroke:#7b1fa2,stroke-width:3px
    style UserLayer fill:#e8f5e9,stroke:#2e7d32
    style HotStorage fill:#ffeb3b,stroke:#f57f17
    style ColdStorage fill:#90caf9,stroke:#1565c0




    